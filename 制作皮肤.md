# 制作一套皮肤

> 面向：想给**灵伴**做一个自定义形象的人。
> **你不需要写代码，也不需要 Unity** —— 只要一个文件夹和几张 PNG。

---

## 30 秒版本

一套皮肤就是一个**文件夹**。最小的能用的皮肤长这样：

```
我的皮肤/
└── idle/
    └── 00.png
```

**就这样。连配置文件都不用写。**

把文件夹放到 `%APPDATA%\DeskSprite\skins\` 下面，或者直接右键点她 →
设置 → 底部「选择…」→ 选中这个文件夹，她**当场**就换了。

---

## 完整结构

下面是一个功能齐全的皮肤，每一行都标了"必须"还是"可选"：

```
我的皮肤/
├── skin.json          ← 可选。不写就用全部默认值（见下）
│
├── idle/              ← 【必须有】她平时循环播放的动作
│   ├── 00.png
│   ├── 01.png
│   └── ...
│
├── hover/             ← 可选。鼠标移到她身上时播
├── press/             ← 可选。按下但还没决定是点是拖的那一瞬间
├── drag/              ← 可选。被拎着走的时候
├── click/             ← 可选。被点一下的反应（**每次点击都从头播** ✓）
│
├── idle_sleep/        ← 可选。「随机待机池」成员，见下面「动作名」
├── idle_cute/         ← 可选。同上，名字随便起
└── idle_1/            ← 可选。同上，纯数字也行
```

### 规则只有三条硬的

| | |
|---|---|
| **要有 `idle/`** | 它是兜底的地基 —— 别的动作缺失都能回落到它，它自己不能缺 |
| **同一个动作里，所有帧必须一样大** | 不一样的话动画会抖 |
| **背景必须透明** | 见下面「图片要求」—— 这条最容易被忽略，而且后果最难看 |

其余全部有默认值 ✓ —— 目录名就是动作名，多一个少一个都不影响加载。

### 一条建议（不是硬要求）：`hover` 尽量别让身体离开原位

`hover` 是"鼠标停在她身上"时播的动作。如果它把身体挪到别处（比如把整条手臂抬起来，
原来那块像素就空了），引擎有一层**输入迟滞**兜着：只要你的鼠标没动过，
就仍然算"鼠标在她身上"，不会被踢出 hover ✓。

所以**不会因此坏掉** ✓。但轮廓稳定仍然值得做，因为：

- **哪些像素吃鼠标是按当前帧算的** —— 轮廓一动，这块区域就跟着变，
  用户会觉得"点击手感飘"，却看不出原因。
- 想表达"她注意到你了"，改**表情、细节、轻微起伏**就够了；
  真要大幅换姿势，尽量让身体重心留在原地。

（这条只是建议 —— 素材长什么样归作者，引擎不替作者做检查。）

---

## 图片要求

| 项 | 要求 | 为什么 |
|---|---|---|
| **格式** | PNG | 只有 PNG |
| **背景** | **必须透明** | 命中和鼠标穿透都靠透明度。不透明的背景会让她变成**一个方块**，而且**整块矩形**都会吃掉鼠标点击（桌面图标点不到） |
| **同一套皮肤的帧尺寸** | 全部一样大 | 窗口 = 帧尺寸 × 缩放。不一致会直接**拒绝加载**并说明原因 |
| **她站在哪** | **脚踩在画布底边**、**重心在水平中点** | 引擎按"底边中点"定位。留白留在脚下 → 她会浮在半空 |
| **建议高度** | **256 ~ 512 像素** | 见下面「多大的图合适」 |
| **软阴影/光晕** | 尽量别有，或者别太淡 | alpha 低于 25% 的像素不算命中，但**很淡的一圈投影**肉眼看不见却会让一大片区域吃鼠标 |

**像素画额外两条 —— 第一条是硬要求**：

1. **必须在 `skin.json` 里写 `"filter": "point"`** ⚠️
   引擎的默认是 `bilinear`（因为这套设计主要面向非像素画），
   **不写就等于声明"我不是像素画"** —— 放大时整只都会糊掉，而且**用户改不了这一项**（它归作者）。
2. 图要**小**（一般 64~128 像素高），靠 `"scale": N` 这个**整数倍**放大到屏幕上。
   非整数倍会让像素大小忽大忽小 —— 看起来就是坏了。

---

## 多大的图合适

**素材的分辨率不决定她在屏幕上多大** ✓ —— 引擎会把图装进一个合理的大小，
用户还能随时用设置里的「角色大小」再调。

```
最终大小 = 图 × 缩放倍率
   缩放倍率 = scale（你声明） 或 displayHeight / 图高（你声明） 或 自动（装进 300 像素高）
```

所以：

- **非像素画**（手绘、AI 生成）：**高度 256~512 就够** ✓。做更大不会更清楚 ——
  她显示出来只有两三百像素高，更大的图只会被自动缩小（还白占内存）
- **像素画**：图要小，然后声明 `"scale": 3` 这样的**整数倍** ✓
- **不声明任何缩放**：引擎自动把图装进 300 像素高（只缩不放）

---

## `skin.json` 模板

**不写完全没问题** ✓。要写的话，下面有两份：一份**可以直接复制**，一份是**全部字段的注释讲解**。

### 可以直接复制的版本

⚠️ 先说最容易踩的一个坑：**JSON 不支持注释。**

`//` 在 JSON 里不是注释，是**语法错误**。Unity 的解析器遇到第一个 `//` 就判定
`skin.json 不是合法 JSON` —— **整个文件一个字段都不读**，皮肤直接加载失败。
所以下面这份没有注释：用空行分组代替注释，空行是合法的，删掉也无所谓。

```json
{
  "formatVersion": 1,
  "name": "我的皮肤",
  "author": "你的名字",
  "description": "一句话说明",

  "frameSize": [256, 384],
  "pivot": [0.5, 0.0],

  "hitTest": "perPixel",
  "defaultFps": 8,
  "actions": [
    { "name": "idle",        "fps": 6 },
    { "name": "idle_sleep",  "fps": 2 },
    { "name": "click",       "fps": 12, "noLoop": true }
  ]
}
```

动作名（`idle`、`click` 这些）**必须和文件夹名一模一样** —— 引擎是按文件夹名找动作的，
`actions` 里只负责给它们配上播放参数。想调哪个就加哪一项，没列进去的照样会加载 ✓。

这份里**故意没有** `scale`、`filter`、`textArea` 三个字段，因为它们是新手最容易写错的：

- 不写 `scale` → 自动把她缩到 300 像素高，**非像素画就该这样** ✓
  像素画才加 `"scale": 3`（那种图本来就小，比如 64×88）。
  ⚠️ **大图千万别加**：`256×384` 的图写 `"scale": 3` 会变成 768×1152，比屏幕还高。
  想让非像素画占一个固定高度，写 `"displayHeight": 320`（物理像素）比 `scale` 靠谱。
- 不写 `filter` → 默认 `bilinear`，插画、手绘、AI 图都对 ✓
  **只有像素画才加** `"filter": "point"`，否则边缘会糊。
- 不写 `textArea` → 数字的**横向中心**会落在帧的左边界上（底边贴 `y=0`），
  所以第一眼看到的现象是"数字卡在左下角、左半截被切掉了"——那是正常的，不是坏了。
  跑起来之后在设置面板里用「余额坐标」挪到合适的位置更直观，挪好按保存，
  **会按皮肤分别记住** ✓
  （格式是 `[左, 下, 宽, 高]`，单位是帧像素；数字横向按这个框居中、竖向贴框底）

### 全部字段（带注释讲解）

⚠️ **这一份不能直接存**（就是上面那个原因），它的作用是告诉你"还能写什么"：

```jsonc
{
  // ── 元信息（都不影响显示，只是给人看的）──
  "formatVersion": 1,              // 建议写：以后格式变了能兼容老皮肤
  "name": "我的皮肤",               // 面板上显示的名字（不写就用文件夹名）
  "author": "你的名字",
  "description": "一句话说明",

  // ── 几何 ──
  "frameSize": [256, 384],         // 一帧多大。**不写就取第一张 PNG 的尺寸**
  "pivot": [0.5, 0.0],             // 锚点，归一化。0.5,0 = 底边中点（默认）

  // ── 缩放（三选一，都不写 = 自动）──
  "scale": 3,                      // 像素画：整数倍
  // "displayHeight": 320,         // 任意画风：屏幕上占多高（物理像素）
  // 都不写 → 自动装进 300 像素高

  // ── 渲染 ──
  "filter": "point",               // 像素画必写；不写默认 bilinear（非像素画）

  // ── 命中 ──
  "hitTest": "perPixel",           // 默认就是 perPixel，一般不用写

  // ── 动作参数 ──
  //    动作名由**文件夹名**决定；数组里这一项的 "name" 只是用来**对上**那个文件夹。
  //    ⚠️ 是数组不是对象 —— 引擎用的 JSON 解析器不支持"以名字为键的对象"。
  //    ⚠️ **只写你要改的那些**：没列进去的动作照样会加载（用 defaultFps 和循环）
  "defaultFps": 8,
  "actions": [
    { "name": "idle",        "fps": 6 },
    { "name": "idle_sleep",  "fps": 2 },
    { "name": "click",       "fps": 12, "noLoop": true }
  ],

  // ── 余额数字放在哪（可选）──
  //    不写 = 帧的左下角。用户随时能在设置里改，所以这里只是个"建议值"
  "textArea": [128, 300, 0, 0]
}
```

### 两个字段要特别说明

**`actions`：每个动作的播放参数都在这里调**

**所有**动画参数都写在 `actions` 里 —— 你不需要去别处找 ✓：

| 参数 | 作用 |
|---|---|
| `fps` | 这个动作**每秒播几帧**（速度）。不写用 `defaultFps`，再默认 8 |
| `noLoop` | 播完一遍就停，不循环（`click` 建议写，见下） |

**它是可选的、而且可以只写一部分** ✓ —— 没列进去的动作照样加载，
用 `defaultFps` 和循环 ✓。所以：

- 想给每个动作不同的节奏 → 写 `actions` ✓
- 所有动作一个速度 → **完全可以不写** ✓

`noLoop` 决定的是"**状态机什么时候离开这个动作**"，**不是**"播不播"：

| | 效果 |
|---|---|
| **`noLoop: true`** | 播完**最后一个**帧就立刻回 `idle` |
| **不写**（= 循环） | 状态机要等 `1.5 ×(帧数 ÷ fps)` 的超时才离开 —— 也就是那一遍会**多播半遍** |

所以：

| 动作 | `noLoop` |
|---|---|
| **`click`** | **建议写** ✓ —— 不写的话，"点一下"的反应会多播半遍才收 |
| **`idle`** | **绝对不要写** ✗ —— 见下面的警告 |
| `hover` / `drag` / `press` | **写不写都一样** ✓ —— 它们什么时候结束由**鼠标**决定，不看动画播完没有 |

**每次点击都从头播** ✓ —— 这一点由引擎保证，不用你操心
（连点几下就是连播几遍开头，不会接着上次播）。

> ⚠️ **`idle` 必须循环**（别写 `noLoop`）。两个后果：
> ① 她会**停在最后一帧**不动；
> ② **随机待机池永远不会触发** —— 引擎是靠"`idle` 绕回第一帧"来数一个完整周期的。
> 引擎会在加载时检查这一条并警告。

**为什么没有 `loop` 而是 `noLoop`？** 因为 JSON 解析器分不清"字段没写"和"写了 false"，
所以反着写，"没写 = false = 循环"正好对上。

**`textArea`**

- `[中心 x, 底边 y, 宽, 高]` —— 数字画在哪、多大**由用户决定**，
  你写的只是**建议值**（用户随时能盖掉，存 `skin.user.json`）
- **x 是数字的"水平中心"**，不是左边缘。因为金额位数会变，
  中心对齐才是"它在原地往两边长"
- **不写就是帧左下角** ✓ —— 那样也能用，用户拖到想要的位置就行
- 后两个数现在**没用**了（保留是为了兼容老皮肤）

---

## 动作名对照表

动作名就是**文件夹名**（大小写不敏感）：

| 什么时候播 | 找哪个名字 | 必须？ | 没有的话 |
|---|---|---|---|
| 平时待机 | `idle` | 🔴 **必须** | —— |
| 鼠标移上去 | `hover` | 可选 | 保持原样 |
| 按下的瞬间 | `press` | 可选 | **不改动作**（避免普通点击闪一下） |
| 被拎着走 | `drag` | 可选 | 回 `idle` |
| 被点一下 | `click` | 可选 | 回 `idle`（**建议 `noLoop`**，见上） |

### 随机待机池：`idle_` 开头的都算 —— **数量没有上限**

**鼠标不理她的时候，她会隔一段时间随机换一个姿势。**
任何以 `idle_` 开头的动作都会自动进这个池子：

```
idle_sleep/    ← 睡觉
idle_cute/     ← 卖萌
idle_stretch/  ← 伸懒腰
idle_1/        ← 名字随便，纯数字也行
```

**想加几个就加几个** ✓ —— **没有数量限制**，引擎也不需要在任何配置里登记它们
（放一个 `idle_xxx/` 文件夹进去就生效 ✓）。一个池子、两个、二十个都行 ✓。

- 池子里的动作**播完一个周期就自动回 `idle`** ✓ —— 想让她睡久一点，就**多画几帧**
- **不会连着播同一个** ✓（否则池子里只有两个动作时会看起来像卡住了）
- 间隔和"要不要随机待机"由用户控制（设置里的「待机动作切换间隔」）
- **一个 `idle_` 都没有 = 关掉这个功能** ✓（她就一直播 `idle`）

---

## 装到哪、怎么试

**两个办法，选一个：**

1. **放进去**：拷到 `%APPDATA%\DeskSprite\skins\我的皮肤\`，重开程序
2. **直接选**（推荐）：右键点她 → 设置 → 底部「选择…」→ 选中你的文件夹 →
   **当场生效，不用重开** ✓

选中的文件夹会被记住（写进 `config.json`），以后启动就用它。

**验证有没有加载成功**：设置面板底部会显示**当前真正加载的那一套**的名字 ✓。
如果选错了，面板上会直接说「这套皮肤用不了：……」。

**改完图要重新加载**：选完文件夹是当场生效的，但**改了一个已经加载的皮肤里的图**，
要重开程序才会重新读（热重载还没做）。

---

## 出问题了先对这几条

| 症状 | 原因 | 怎么办 |
|---|---|---|
| **她变成一个方块** | 背景没抠 | 抠图，让背景**完全透明** |
| **她浮在半空** | 脚下留了空白 | 把多余的空白裁掉（她应该**脚踩画布底边**） |
| **她站歪了** | 水平不居中 | 让她的重心对准画布水平中点 |
| **动画在抖** | 同一个动作里帧尺寸不一致 | 全部裁成一样大 |
| **放大后很糊** | 像素画没声明滤波 | 写 `"filter": "point"` |
| **点不到她 / 旁边空白也吃鼠标** | 背景不透明，或者有一圈很淡的投影 | 抠干净 |
| **数字不见了** | 字号太小 / 位置在画面外 | 设置里调「余额坐标」和「字号」 |
| **面板说「这套皮肤用不了」** | 选错层级了 | 要选**直接装着 `skin.json` 和 `idle/` 的那一层**；日志里会提示"里面那个看起来对" |
| **选完了还是老样子** | 选的那个目录里没有 `idle/` | 面板会直接告诉你原因 |

---

## 那些"写了但暂时没用"的字段

诚实列出来，免得你白写：

| 字段 | 状态 |
|---|---|
| `allowMotion` | 预留：放行刻意的大位移（引擎的自动检查还没做） |
| `pivotOffset` | 预留：每个动作单独偏移 |
| `actions` 里的 `fallback` | 预留：自定义兜底链（现在固定回 `idle`） |
| `hitTest` 的 `bounds` / `none` | 预留：现在只有 `perPixel` 一种 |

这些字段写了**不会报错**，但也不会生效 —— 加载时会记进日志的"忽略"清单里 ✓。

---

## 想让别人也能用你的皮肤

整个文件夹拷过去就行 —— **它不依赖任何绝对路径** ✓。
（注意别把 `skin.user.json` 一起打进去，那是**用户自己的**调整记录，
存在 `%APPDATA%\DeskSprite\skins\` 下面，不在你的皮肤文件夹里 ✓。）

这份手册就是全部规则 —— 皮肤格式没有"隐藏章节"，照着做就行 ✓。
